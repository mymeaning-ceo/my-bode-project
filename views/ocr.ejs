<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>OCR 변환</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/main.css">
</head>
<body class="bg-light">
  <%- include('nav.ejs') %>

  <div class="container my-5">
    <h2 class="mb-4 fw-bold">🖼️ 이미지 텍스트 추출</h2>
    <form action="/ocr" method="POST" enctype="multipart/form-data" class="d-flex flex-wrap gap-2 mb-4">
      <input type="file" name="image" accept="image/*" class="form-control" required>
      <button type="submit" class="btn btn-primary">추출</button>
    </form>

    <% if (logs && logs.length > 0) { %>
      <h5 class="mb-3">OCR 로그</h5>
      <pre class="p-3 bg-white border rounded"><%= logs.join('\n') %></pre>
    <% } %>

    <% if (text) { %>
      <h5 class="mt-4 mb-3">추출된 텍스트</h5>
      <pre class="p-3 bg-white border rounded"><%= text %></pre>
      <% if (id) { %>
        <form action="/voucher/import/<%= id %>" method="POST" class="mt-3">
          <button type="submit" class="btn btn-success">전표에 저장</button>
        </form>
      <% } %>

    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
