<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>재고 관리</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/main.css">
  <style>
    td.low-stock {
      background-color: #ffe5e5 !important;
      font-weight: bold;
      color: #d8000c;
    }
  </style>
</head>
<body class="bg-light">

  <%- include('nav.ejs') %>

  <div class="container my-5">
    <h2 class="mb-4 fw-bold">📦 재고 관리</h2>
    <div class="alert alert-info mb-4">
      헤더를 클릭하거나 위 셀렉트 박스로 정렬할 수 있습니다.
    </div>

    <% if (성공메시지) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= 성공메시지 %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <!-- 업로드 및 초기화 -->
    <div class="action-form mb-4 d-flex gap-2 flex-wrap">
      <form action="/stock/upload" method="POST" enctype="multipart/form-data" class="d-flex gap-2 flex-nowrap">
        <input type="file" name="excelFile" accept=".xlsx, .xls" class="form-control" required>
        <button type="submit" class="btn btn-success btn-upload">엑셀 업로드</button>
      </form>
      <form action="/stock/delete-all" method="POST" onsubmit="return confirm('정말 모든 데이터를 삭제하시겠습니까?')">
        <button type="submit" class="btn btn-danger btn-reset">데이터 초기화</button>
      </form>
    </div>

    <!-- 검색 -->
    <form action="/stock/search" method="GET" class="input-group mb-4">
      <input type="text" name="keyword" class="form-control" placeholder="상품명 또는 상품코드 입력" value="<%= 검색어 || '' %>">
      <button class="btn btn-outline-primary" type="submit">검색</button>
    </form>

    <% if (결과 && 결과.length > 0) { %>
      <p class="text-muted">🔎 총 <strong><%= 결과.length %></strong>건의 결과가 있습니다.</p>
    <% } %>

    <% const 제외필드   = ['_id', 'type', 'unit', 'first_release_date']; %>
    <% const 원하는순서 = ['item_code', 'item_name', 'size_color', 'qty', 'allocation']; %>
    <% const headerMap = {
      item_code: '품번',
      item_name: '품명',
      size_color: '호칭-색상',
      qty: '재고수량',
      allocation: '할당'
    }; %>
    <% const 표시필드 = 원하는순서.filter(k => (필드 || []).includes(k) && !제외필드.includes(k)); %>

    <div class="table-responsive">
      <table class="table table-bordered table-hover bg-white align-middle auto-width">
        <thead class="table-light text-center">
          <% if (표시필드.length > 0) { %>
            <tr id="sortRow">
              <% 표시필드.forEach((key, idx) => { %>
                <th>
                  <select class="form-select form-select-sm sort-select" data-index="<%= idx %>">
                    <option value="">-</option>
                    <option value="asc">오름차순▲</option>
                    <option value="desc">내림차순▼</option>
                  </select>
                </th>
              <% }) %>
            </tr>
            <tr>
              <% 표시필드.forEach((key, idx) => { %>
                <th class="sortable" data-index="<%= idx %>">
                  <%= headerMap[key] || key %> <span class="sort-indicator"></span>
                </th>
              <% }) %>
            </tr>
          <% } else { %>
            <tr><th>📂 No Data</th></tr>
          <% } %>
        </thead>
        <tbody>
          <% if (결과 && 결과.length > 0) { %>
            <% 결과.forEach(행 => { %>
              <tr>
                <% 표시필드.forEach(key => {
                     const rawVal = 행[key];
                     const numVal = typeof rawVal === 'string'
                       ? Number(rawVal.replace(/,/g, ''))
                       : rawVal;
                %>
                  <td class="<%= (key === 'qty' || key === '재고량') && numVal !== null && numVal <= 10 ? 'low-stock' : '' %>">
                    <%= typeof rawVal === 'number' ? rawVal.toLocaleString('ko-KR') : rawVal %>
                  </td>
                <% }) %>
              </tr>
            <% }) %>
          <% } else { %>
            <tr>
              <td colspan="100%" class="text-center text-muted">📭 재고가 없습니다.</td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>

    <% if (전체페이지수 > 1) { %>
      <nav class="mt-4" aria-label="페이지네이션">
        <ul class="pagination justify-content-center">
          <% const maxPagesToShow = 10; %>
          <% const startPage = Math.floor((현재페이지 - 1) / maxPagesToShow) * maxPagesToShow + 1; %>
          <% const endPage = Math.min(startPage + maxPagesToShow - 1, 전체페이지수); %>
          <% const baseUrl = 검색어 ? ('/stock/search?keyword=' + encodeURIComponent(검색어) + '&page=') : '/stock?page='; %>

          <% if (startPage > 1) { %>
            <li class="page-item">
              <a class="page-link" href="<%= baseUrl + (startPage - 1) %>">« 이전</a>
            </li>
            <li class="page-item disabled"><span class="page-link">...</span></li>
          <% } %>

          <% for (let i = startPage; i <= endPage; i++) { %>
            <li class="page-item <%= i === 현재페이지 ? 'active' : '' %>">
              <a class="page-link" href="<%= baseUrl + i %>"><%= i %></a>
            </li>
          <% } %>

          <% if (endPage < 전체페이지수) { %>
            <li class="page-item disabled"><span class="page-link">...</span></li>
            <li class="page-item">
              <a class="page-link" href="<%= baseUrl + (endPage + 1) %>">다음 »</a>
            </li>
          <% } %>
        </ul>
      </nav>
    <% } %>

  <script>
    const getCellValue = (tr, idx) => tr.children[idx].textContent.trim();
    const toNumber = (v) => {
      const n = parseFloat(v.replace(/,/g, ''));
      return isNaN(n) ? null : n;
    };
    const comparer = (idx, asc) => (a, b) => {
      const v1 = getCellValue(asc ? a : b, idx);
      const v2 = getCellValue(asc ? b : a, idx);
      const n1 = toNumber(v1), n2 = toNumber(v2);
      if (n1 !== null && n2 !== null) return n1 - n2;
      if (n1 === null && n2 === null) return 0;
      if (n1 === null) return 1;
      return -1;
    };

    document.querySelectorAll('th.sortable').forEach(th => th.addEventListener('click', () => {
      const table = th.closest('table');
      const asc = th.dataset.asc !== 'true';
      Array.from(table.querySelectorAll('tbody tr'))
        .sort(comparer(parseInt(th.dataset.index), asc))
        .forEach(tr => table.tBodies[0].appendChild(tr));

      document.querySelectorAll('th.sortable').forEach(t => {
        t.dataset.asc = '';
        t.querySelector('.sort-indicator').textContent = '';
      });

      th.dataset.asc = asc ? 'true' : 'false';
      th.querySelector('.sort-indicator').textContent = asc ? '▲' : '▼';
      document.querySelectorAll('.sort-select').forEach(sel => sel.value = '');
    }));

    document.querySelectorAll('.sort-select').forEach(sel => {
      sel.addEventListener('change', () => {
        if (!sel.value) return;
        const table = sel.closest('table');
        const idx = parseInt(sel.dataset.index);
        const asc = sel.value === 'asc';

        Array.from(table.querySelectorAll('tbody tr'))
          .sort(comparer(idx, asc))
          .forEach(tr => table.tBodies[0].appendChild(tr));

        document.querySelectorAll('th.sortable').forEach(t => {
          t.dataset.asc = '';
          t.querySelector('.sort-indicator').textContent = '';
        });

        document.querySelectorAll('.sort-select').forEach(other => {
          if (other !== sel) other.value = '';
        });

        const th = table.querySelector(`th.sortable[data-index="${idx}"]`);
        if (th) {
          th.dataset.asc = asc ? 'true' : 'false';
          th.querySelector('.sort-indicator').textContent = asc ? '▲' : '▼';
        }
      });
    });
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
