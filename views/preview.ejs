<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>엑셀 미리보기</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4 bg-light">
  <h2 class="mb-4">🔍 엑셀 데이터 미리보기</h2>

  <form action="/stock/mapping" method="POST">
    <table class="table table-bordered table-hover">
      <thead class="table-light text-center">
        <tr>
          <% 필드.forEach((key, idx) => { %>
            <th>
              <select class="form-select form-select-sm" name="map[<%= idx %>]">
                <option value="">선택 안함</option>
                <option value="품목코드">품목코드</option>
                <option value="상품명">상품명</option>
                <option value="사이즈">사이즈</option>
                <option value="재고량">재고량</option>
                <option value="DC율">DC율</option>
              </select>
              <div><small><%= key %></small></div>
            </th>
          <% }) %>
        </tr>
      </thead>
      <tbody>
        <% 데이터.forEach(row => { %>
          <tr>
            <% 필드.forEach(key => { %>
              <td><%= row[key] %></td>
            <% }) %>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <button class="btn btn-primary">매핑 저장</button>
  </form>
</body>
</html>
